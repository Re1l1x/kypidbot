- Делаем ли дружеские встречи?
    P.S. дружеские встречи -- технически те, в которых разрешены однополые коннекты.
    + привлечет потенциально больше людей (охватит);
    + мы сможем больше людей сматчить в пару, если будет перевес одного пола;
    - теряется такой чисто романтический вайб 14го февраля.

- Собрать места для встреч (таблица `places`);
- Придумать как пара найдет друг друга, если вдруг место для встречи будет людным;
- Как обрабатываем тех, кто остался без пары?
- По команде `/match` -- формируются пары, сделать команду `/meet`, чтобы разослать всем сформированным парам сообщение о свиданке (заполнить таблицу `dates`);
- По команде `/match` после формирования пар, так же нужно:
    * выбрать для пары место встречи;
    * выбрать для пары время встречи;
    * мб какое-то кодовое слово для каждой пары (?)
